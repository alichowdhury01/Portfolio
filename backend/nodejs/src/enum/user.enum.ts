enum AccountType {
    ADMIN = 'admin',
    MANAGER = 'manager',
    TENANT = 'tenant',
    EMPLOYEE = 'employee',
}

enum Status {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export default { AccountType, Status };